{
  "name": "finanzdashboard",
  "version": "1.0.0",
  "description": "Finanzdashboard - Portfolio, Sparpläne und Signalquellen in einer Ansicht. Statisch, ohne Build-Schritt.",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "npx http-server -p 8080 -c-1 -o",
    "signals": "node scripts/fetch-signals.mjs",
    "check": "node --check src/app.js && node --check scripts/fetch-signals.mjs"
  },
  "license": "MIT"
}
